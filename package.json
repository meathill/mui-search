{
  "name": "mui-search",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "format": "biome format . --write",
    "lint": "biome check .",
    "typecheck": "pnpm -r run typecheck",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.10",
    "typescript": "^5.9.3",
    "vitest": "^4.1.2"
  }
}
